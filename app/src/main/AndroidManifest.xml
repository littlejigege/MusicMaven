<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.qg.musicmaven">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".ui.mainpage.TestMainActivity"
            android:label="@string/title_activity_test_main" />

        <activity-alias
            android:name=".Launcher"
            android:label="@string/app_name"
            android:targetActivity=".ui.mainpage.TestMainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity-alias>

        <activity android:name=".rlogin.LoginSuccessActivity" />
        <activity
            android:name=".rlogin.RegisterActivity"
            android:theme="@style/Translucent" />
        <activity android:name=".rlogin.LoginActivity" />
        <activity android:name=".ui.cloudpage.singerpage.SingerActivity" />
        <activity android:name=".ui.searchpage.SearchActivity" />
        <activity
            android:name=".ui.dreampage.WishActivity"
            android:theme="@style/Theme.AppCompat.DayNight.DarkActionBar"
            android:label="愿望列表"/>
    </application>

</manifest>